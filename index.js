//write a function that takes 3 parameters (name, height, country)
//write an alert in the function that prints out the inputs
//call the function and pass the 3 parameters into it
function myFunction(Name, Height, Country) {
    console.log(Name + ',' + Height + ',' + Country);
}

myFunction('Kamilah W.', '62cm', 'USA');

